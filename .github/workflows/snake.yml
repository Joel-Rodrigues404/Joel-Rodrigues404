# name: Generate Snake Animation

# on:
#   schedule:
#     - cron: "0 0 * * *" # Gera o Snake diariamente à meia-noite UTC
#   workflow_dispatch: # Permite executar manualmente

# jobs:
#   generate:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repositório
#         uses: actions/checkout@v3

#       - name: Gerar Snake
#         uses: platane/snk@v3
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           github_user_name: Joel-Rodrigues404 # Substitua pelo seu nome de usuário
#           outputs: dist/github-snake-dark.svg

#       - name: Deploy para o Branch 'output'
#         uses: crazy-max/ghaction-github-pages@v2
#         with:
#           node-version: '20'
#           personal_token: ${{ secrets.GH_PAT }}
#           publish_dir: ./output
#           target_branch: output
#           build_dir: dist

name: Generate Snake

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - uses: Platane/snk@master
        id: snake-gif
        with:
          github_user_name: Joel-Rodrigues404 
          gif_out_path: dist/github-contribution-grid-snake.gif
          svg_out_path: dist/github-contribution-grid-snake.svg

      - run: git status

      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: master
          force: true

      - uses: crazy-max/ghaction-github-pages@v2.1.3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}




    

    

    






